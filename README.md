# Garment Floor Tracker — Web App → Real Android APK

This folder is a complete, self-contained web app (index.html + manifest + icons).
It stores all your data in the phone's browser storage — works fully offline once loaded.

See the chat response for the full step-by-step: host it for free (GitHub Pages),
then run it through PWABuilder.com to generate a signed, installable .apk —
no Android Studio, no coding.
